# 快速切换模型提供者

## 🚀 快速切换

### 切换到API云上模型（默认）

编辑 `config/config.yaml`:

```yaml
model:
  provider: "api"
  api_base: "https://newapi.3173721.xyz/v1/chat/completions"
  api_key: "your-api-key"
  model_name: "qwen3-max"
```

### 切换到本地部署模型

编辑 `config/config.yaml`:

```yaml
model:
  provider: "local"
  model_path: "/path/to/your/model"
  device: "cuda"  # 或 "cpu"
  model_name: "qwen-7b-chat"
```

## ✅ 验证

运行测试脚本：

```bash
python test_model_provider.py
```

应该看到：
- 当前配置的提供者类型
- LLMClient创建成功
- 提供者类型（APIModelProvider 或 LocalModelProvider）

## 📝 完整配置示例

参考：
- `config/config.yaml` - 当前配置
- `config/config.local.example.yaml` - 本地模型配置示例
- `MODEL_PROVIDER_CONFIG.md` - 详细配置文档
