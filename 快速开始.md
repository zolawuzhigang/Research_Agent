# Research Agent Demo - 快速开始

## 🚀 三步运行

### 步骤1：安装依赖
```bash
pip install -r requirements.txt
```

### 步骤2：启动服务
```bash
# 方式1：HTTP服务（推荐）
python run_server.py

# 方式2：控制台交互
python run_console.py
```

### 步骤3：测试
```bash
# 如果启动了HTTP服务，在另一个终端运行：
python test_api.py
```

## 📝 详细说明

### HTTP服务模式

**启动**:
```bash
python run_server.py
```

服务地址: `http://localhost:8000`

**测试**:
```bash
# 使用测试脚本
python test_api.py

# 或使用curl
curl -X POST \
     -H "Authorization: Bearer test_token" \
     -H "Content-Type: application/json" \
     -d '{"question": "法国首都在哪里？"}' \
     "http://localhost:8000/api/v1/predict"
```

**响应格式**:
```json
{
  "answer": "巴黎"
}
```

### 控制台交互模式

```bash
python run_console.py
```

然后输入问题，例如：
```
你: 法国首都在哪里？
Agent: 巴黎
[置信度: 0.85]
```

## ⚙️ 配置说明

LLM配置已内置，默认使用：
- API: `https://newapi.3173721.xyz/v1/chat/completions`
- Key: `sk-DwBE5H6xxCV6I7i0q8v6rq3ZHauPuSq6fWVerxu7gJ9DmQoz`
- 模型: `qwen3-max`

如需修改，编辑 `config/config.yaml` 或设置环境变量。

## 🐛 常见问题

**问题1**: 端口被占用
```bash
# 修改 run_server.py 中的端口号
uvicorn.run(..., port=8001)
```

**问题2**: 依赖安装失败
```bash
# 升级pip
python -m pip install --upgrade pip
# 重新安装
pip install -r requirements.txt
```

**问题3**: 导入错误
```bash
# 确保在项目根目录运行
cd c:\Users\bigda\Desktop\ailiyunAgent
python run_server.py
```

## 📚 更多信息

- 详细文档: `RUN_GUIDE.md`
- API文档: `README_DEMO.md`
- 技术文档: `TECHNICAL_DESIGN.md`
