# 快速修复：启动卡住问题

## ⏱️ 正常启动时间

- **标准模式** (`run_server.py`): **3-10秒**
- **快速模式** (`run_server_fast.py`): **< 2秒**（Agent延迟初始化）

如果超过30秒还在转圈，说明有问题。

## 🚨 问题：IDEA运行后一直转圈

### 立即解决方案

#### 方案1：使用快速启动版本（最简单）✅

```bash
python run_server_fast.py
```

这个版本会在**第一次请求时**才初始化Agent，启动速度很快。

#### 方案2：修改启动超时

如果必须使用原版本，编辑 `src/api/http_server.py`：

找到 `startup_event()` 函数，确保有超时保护（代码已优化，应该已经有了）。

#### 方案3：检查网络

```bash
# 测试LLM API是否可访问
curl -X POST https://newapi.3173721.xyz/v1/chat/completions \
     -H "Authorization: Bearer sk-DwBE5H6xxCV6I7i0q8v6rq3ZHauPuSq6fWVerxu7gJ9DmQoz" \
     -H "Content-Type: application/json" \
     -d '{"model":"qwen3-max","messages":[{"role":"user","content":"test"}]}'
```

如果这个命令也卡住，说明网络问题。

## 🔍 诊断步骤

### 1. 查看日志输出

运行 `python run_server.py` 后，观察日志：
- 如果卡在 "正在初始化Research Agent..."
- 如果卡在 "步骤1: 加载配置..."
- 如果卡在 "步骤2: 配置加载完成..."
- 如果卡在 "步骤3: 初始化Agent..."

### 2. 添加调试日志

在 `src/api/http_server.py` 的 `startup_event()` 中添加：

```python
logger.info("🔵 开始初始化...")
# ... 代码 ...
logger.info("🔵 完成步骤1")
# ... 代码 ...
logger.info("🔵 完成步骤2")
```

### 3. 使用简化模式

临时测试：
```python
# 在 startup_event() 中
agent = AgentOrchestrator(config={}, use_multi_agent=False)  # 禁用多Agent
```

## ✅ 推荐方案

**使用快速启动版本**：

```bash
# 1. 启动服务（快速）
python run_server_fast.py

# 2. 在另一个终端测试
python test_api.py
```

这样启动会很快，Agent在第一次请求时才初始化。

## 📝 已优化的内容

代码已优化，包含：
- ✅ 超时保护（30秒）
- ✅ 异步初始化
- ✅ 错误恢复
- ✅ 延迟初始化支持

如果还是卡住，使用 `run_server_fast.py` 即可。
